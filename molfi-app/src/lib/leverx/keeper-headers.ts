/** Shared with keeper `KEEPER_API_KEY_HEADER` when ops routes use API key auth. */
export const KEEPER_API_KEY_HEADER = "x-keeper-api-key";

/** Bearer token issued after a signed keeper request (3-day session). */
export const KEEPER_APP_AUTH_HEADER = "authorization";
