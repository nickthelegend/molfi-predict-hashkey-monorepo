/**
 * No-op. Enoki / Google zkLogin was a Sui-only feature and is not available on
 * HashKey Chain. Kept as an empty component so existing render sites compile.
 */
export function EnokiWalletsRegistrar() {
  return null;
}
